# SECURITY WARNING: keep the secret key used in production secret!
# Generate a new key for production.
# You can use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY='your-django-secret-key'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Allowed hosts for the Django app (space-separated)
ALLOWED_HOSTS='127.0.0.1 localhost'

# Database Configuration (Example for PostgreSQL)
DATABASE_URL='postgres://user:password@host:port/dbname'

# Social Auth Keys (Example for Google)
# Get these from the Google Cloud Console
SOCIAL_AUTH_GOOGLE_OAUTH2_KEY='your-google-client-id'
SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET='your-google-client-secret'

# Cloudinary Storage for Media Files (Optional but recommended for production)
CLOUDINARY_URL='cloudinary://api_key:api_secret@cloud_name'
